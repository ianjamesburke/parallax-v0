"""Bundled agent skills shipped with the parallax CLI.

Agents that drive the parallax CLI from the outside (e.g. narrative-parallax's
orchestrator) load these MD files via `importlib.resources.files("parallax")`
to keep their guidance in sync with the installed CLI version.
"""
